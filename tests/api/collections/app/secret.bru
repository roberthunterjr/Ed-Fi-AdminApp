meta {
  name: app/secret/<id> - Bad Request (400)
  type: http
  seq: 2
  tags: App
}

get {
  url: {{BASE_URL}}/secret/test-secret-id
}

tests {
  test("app/secret/<id> - Bad Request (400)", function() {
    const status = res.getStatus();
    expect(status).to.equal(400);
  });
}
