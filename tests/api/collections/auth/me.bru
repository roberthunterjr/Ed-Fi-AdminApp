meta {
  name: auth/me - OK (200)
  type: http
  seq: 1
  tags: Auth
}

headers {
  Authorization: Bearer {{ACCESS_TOKEN}}
}

get {
  url: {{BASE_URL}}/auth/me
}

tests {
  test("auth/me - OK (200)", function() {
    const status = res.getStatus();
    expect(status).to.equal(200);
  });
}
