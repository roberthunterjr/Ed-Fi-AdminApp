meta {
  name: auth/cache/<team_id> - OK (200)
  type: http
  seq: 5
  tags: Auth
}

headers {
  Authorization: Bearer {{ACCESS_TOKEN}}
}

get {
  url: {{BASE_URL}}/auth/cache/{{TEAM_ID}}
}

tests {
  test("auth/cache/<team_id> - OK (200)", function() {
    const status = res.getStatus();
    expect(status).to.equal(200);
  });
}
