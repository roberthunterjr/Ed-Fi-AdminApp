meta {
  name: auth/cache - OK (200)
  type: http
  seq: 4
  tags: Auth
}

headers {
  Authorization: Bearer {{ACCESS_TOKEN}}
}

get {
  url: {{BASE_URL}}/auth/cache
}

tests {
  test("auth/cache - OK (200)", function() {
    const status = res.getStatus();
    expect(status).to.equal(200);
  });
}
