# This compose file defines the Ed-Fi ODS / API services required by the
# Ed-Fi Admin App for local development and testing.

services:
  # Ed-Fi v7.x - Single tenant
  odsV7-adminV2-single-db-ods:
    build:
      context: ./DB-Ods
    image: edfiadminapp/db-ods:local
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_PORT: "${POSTGRES_PORT:-5432}"
      TPDM_ENABLED: '${TPDM_ENABLED:-true}'
      MINIMAL_SQL_PATH: "${MINIMAL_SQL_PATH:-/var/opt/pgsql/data/sql-backups/EdFi.Ods.Minimal.Template.sql}"
      POPULATED_SQL_PATH: "${POPULATED_SQL_PATH:-/var/opt/pgsql/data/sql-backups/EdFi.Ods.Populated.Template.sql}"
      EDFI_ODS_DATASET: "${EDFI_ODS_DATASET:-minimal}"
    restart: always
    volumes:
      - vol-odsV7-adminV2-single-db-ods:/var/lib/postgresql/data
      - ${SQL_BACKUPS_FOLDER:-./db-backup}:/var/opt/pgsql/data/sql-backups/:ro
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER} -h localhost -p ${POSTGRES_PORT:-5432}']
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV2-single-db-admin:
    image: edfialliance/ods-admin-api-db:${ADMIN_DB_TAG_7X}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      # The following variables ared used by bootstrap.sh to set up the ODS
      # connection string in the OdsInstances table.
      ODS_PGBOUNCER_PORT: 5432
      ODS_POSTGRES_HOST: odsV7-adminV2-single-db-ods
      NPG_POOLING_ENABLED: '${NPG_POOLING_ENABLED:-true}'
      NPG_API_MAX_POOL_SIZE_ODS: '${NPG_API_MAX_POOL_SIZE_ODS:-20}'
    volumes:
      - ./settings/bootstrap.sh:/docker-entrypoint-initdb.d/2-bootstrap.sh
      - vol-odsV7-adminV2-single-db-admin:/var/lib/postgresql/data
    restart: always
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready  -U ${POSTGRES_USER}']
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV2-single-api:
    image: edfialliance/ods-api-web-api:${ODS_API_TAG_7X}
    environment:
      ADMIN_POSTGRES_HOST: odsV7-adminV2-single-db-admin
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_PORT: 5432
      PATH_BASE: '${ODS_V7_ADMIN_V2_SINGLE_API_VIRTUAL_NAME}'
      TPDM_ENABLED: '${TPDM_ENABLED:-true}'
      ODS_WAIT_POSTGRES_HOSTS: 'odsV7-adminV2-single-db-ods '
      NPG_POOLING_ENABLED: '${NPG_POOLING_ENABLED:-true}'
      NPG_API_MAX_POOL_SIZE_ODS: '${NPG_API_MAX_POOL_SIZE_ODS:-20}'
      NPG_API_MAX_POOL_SIZE_ADMIN: '${NPG_API_MAX_POOL_SIZE_ADMIN:-20}'
      NPG_API_MAX_POOL_SIZE_SECURITY: '${NPG_API_MAX_POOL_SIZE_SECURITY:-20}'
      NPG_API_MAX_POOL_SIZE_MASTER: '${NPG_API_MAX_POOL_SIZE_MASTER:-20}'
      ODS_CONNECTION_STRING_ENCRYPTION_KEY: '${ODS_CONNECTION_STRING_ENCRYPTION_KEY}'
      ASPNETCORE_ENVIRONMENT: 'docker'
      FeatureManagement__MultiTenancy: false
      ApiSettings__OdsContextRouteTemplate: '{instanceId}'
    volumes:
      - ${LOGS_FOLDER}:/app/logs
    depends_on:
      - odsV7-adminV2-single-db-ods
      - odsV7-adminV2-single-db-admin
    restart: always
    hostname: ${ODS_V7_ADMIN_V2_SINGLE_API_VIRTUAL_NAME}
    healthcheck:
      test: ${ODS_V7_ADMIN_V2_SINGLE_API_HEALTHCHECK}
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV2-single-adminapi:
    image: edfialliance/ods-admin-api:${ADMIN_API_TAG_7X}
    environment:
      ADMIN_POSTGRES_HOST: odsV7-adminV2-single-db-admin
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      ASPNETCORE_ENVIRONMENT: docker
      AppSettings__DatabaseEngine: PostgreSql
      AppSettings__PathBase: '${ODS_V7_ADMIN_V2_SINGLE_ADMIN_API_VIRTUAL_NAME}'
      Authentication__Authority: https://localhost/${ODS_V7_ADMIN_V2_SINGLE_ADMIN_API_VIRTUAL_NAME}
      Authentication__IssuerUrl: https://localhost/${ODS_V7_ADMIN_V2_SINGLE_ADMIN_API_VIRTUAL_NAME}
      Authentication__SigningKey: ${SIGNING_KEY}
      Authentication__AllowRegistration: true
      AppSettings__DefaultPageSizeOffset: ${PAGING_OFFSET:-0}
      AppSettings__DefaultPageSizeLimit: ${PAGING_LIMIT:-25}
      AppSettings__MultiTenancy: false
      AppSettings__EncryptionKey: ${ODS_CONNECTION_STRING_ENCRYPTION_KEY}
      SwaggerSettings__EnableSwagger: true
      SwaggerSettings__DefaultTenant: ${SWAGGER_DEFAULT_TENANT:-tenant1}
      Log4NetCore__Log4NetConfigFileName: ./log4net/log4net.config
      EnableDockerEnvironment: true
      AppSettings__AllowedOrigins: ${ALLOWED_ORIGINS:-https://localhost}
      AppSettings__EnableApplicationResetEndpoint: ${ENABLE_APPLICATION_RESET_ENDPOINT:-true}
      AppSettings__adminApiMode: v2
      ConnectionStrings__EdFi_Admin: 'host=odsV7-adminV2-single-db-admin;port=5432;username=${POSTGRES_USER};password=${POSTGRES_PASSWORD};database=EdFi_Admin;'
      ConnectionStrings__EdFi_Security: 'host=odsV7-adminV2-single-db-admin;port=5432;username=${POSTGRES_USER};password=${POSTGRES_PASSWORD};database=EdFi_Security;'
      ConnectionStrings__EdFi_Ods: 'host=odsV7-adminV2-single-db-ods;port=${POSTGRES_PORT:-5432};username=${POSTGRES_USER};password=${POSTGRES_PASSWORD};database=EdFi_Ods;pooling=true'
      ConnectionStrings__EdFi_Master: 'host=odsV7-adminV2-single-db-ods;port=${POSTGRES_PORT:-5432};username=${POSTGRES_USER};password=${POSTGRES_PASSWORD};database=postgres;pooling=true'
      EdFiApiDiscoveryUrl: ${ODS_V7_ADMIN_V2_SINGLE_API_DISCOVERY_URL}
    volumes:
      - ./settings/log4net.config:/app/log4net/log4net.config
    depends_on:
      - odsV7-adminV2-single-db-admin
      - odsV7-adminV2-single-db-ods
    restart: always
    hostname: ${ODS_V7_ADMIN_V2_SINGLE_ADMIN_API_VIRTUAL_NAME}
    healthcheck:
      test: ${ODS_V7_ADMIN_V2_SINGLE_ADMIN_API_HEALTHCHECK}
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  # Ed-Fi v7.x - Multi tenants
  odsV7-adminV2-tenant1-db-ods:
    build:
      context: ./DB-Ods
    image: edfiadminapp/db-ods:local
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_PORT: "${POSTGRES_PORT:-5432}"
      TPDM_ENABLED: '${TPDM_ENABLED:-true}'
      MINIMAL_SQL_PATH: "${MINIMAL_SQL_PATH:-/var/opt/pgsql/data/sql-backups/EdFi.Ods.Minimal.Template.sql}"
      POPULATED_SQL_PATH: "${POPULATED_SQL_PATH:-/var/opt/pgsql/data/sql-backups/EdFi.Ods.Populated.Template.sql}"
      EDFI_ODS_DATASET: "${EDFI_ODS_DATASET:-minimal}"
    restart: always
    volumes:
      - vol-odsV7-adminV2-tenant1-db-ods:/var/lib/postgresql/data
      - ${SQL_BACKUPS_FOLDER:-./db-backup}:/var/opt/pgsql/data/sql-backups/:ro
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER} -h localhost -p ${POSTGRES_PORT:-5432}']
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV2-tenant2-db-ods:
    build:
      context: ./DB-Ods
    image: edfiadminapp/db-ods:local
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_PORT: "${POSTGRES_PORT:-5432}"
      TPDM_ENABLED: '${TPDM_ENABLED:-true}'
      MINIMAL_SQL_PATH: "${MINIMAL_SQL_PATH:-/var/opt/pgsql/data/sql-backups/EdFi.Ods.Minimal.Template.sql}"
      POPULATED_SQL_PATH: "${POPULATED_SQL_PATH:-/var/opt/pgsql/data/sql-backups/EdFi.Ods.Populated.Template.sql}"
      EDFI_ODS_DATASET: "${EDFI_ODS_DATASET:-minimal}"
    restart: always
    volumes:
      - vol-odsV7-adminV2-tenant2-db-ods:/var/lib/postgresql/data
      - ${SQL_BACKUPS_FOLDER:-./db-backup}:/var/opt/pgsql/data/sql-backups/:ro
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER} -h localhost -p ${POSTGRES_PORT:-5432}']
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV2-tenant1-db-admin:
    image: edfialliance/ods-admin-api-db:${ADMIN_DB_TAG_7X}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      # The following variables ared used by bootstrap.sh to set up the ODS
      # connection string in the OdsInstances table.
      ODS_PGBOUNCER_PORT: 5432
      ODS_POSTGRES_HOST: odsV7-adminV2-tenant1-db-ods
      NPG_POOLING_ENABLED: '${NPG_POOLING_ENABLED:-true}'
      NPG_API_MAX_POOL_SIZE_ODS: '${NPG_API_MAX_POOL_SIZE_ODS:-20}'
      TENANT_IDENTIFIER: tenant1
    volumes:
      - ./settings/bootstrap.sh:/docker-entrypoint-initdb.d/2-bootstrap.sh
      - vol-odsV7-adminV2-tenant1-db-admin:/var/lib/postgresql/data
    restart: always
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready  -U ${POSTGRES_USER}']
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV2-tenant2-db-admin:
    image: edfialliance/ods-admin-api-db:${ADMIN_DB_TAG_7X}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      ODS_PGBOUNCER_PORT: 5432
      # The following variables ared used by bootstrap.sh to set up the ODS
      # connection string in the OdsInstances table.
      ODS_POSTGRES_HOST: odsV7-adminV2-tenant2-db-ods
      NPG_POOLING_ENABLED: '${NPG_POOLING_ENABLED:-true}'
      NPG_API_MAX_POOL_SIZE_ODS: '${NPG_API_MAX_POOL_SIZE_ODS:-20}'
      TENANT_IDENTIFIER: tenant2
    volumes:
      - ./settings/bootstrap.sh:/docker-entrypoint-initdb.d/2-bootstrap.sh
      - vol-odsV7-adminV2-tenant2-db-admin:/var/lib/postgresql/data
    restart: always
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready  -U ${POSTGRES_USER}']
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV2-multi-api:
    image: edfialliance/ods-api-web-api:${ODS_API_TAG_7X}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_PORT: 5432
      PATH_BASE: '${ODS_V7_ADMIN_V2_MULTI_API_VIRTUAL_NAME}'
      TPDM_ENABLED: '${TPDM_ENABLED:-true}'
      ODS_WAIT_POSTGRES_HOSTS: 'odsV7-adminV2-tenant1-db-ods odsV7-adminV2-tenant2-db-ods '
      NPG_POOLING_ENABLED: '${NPG_POOLING_ENABLED:-true}'
      NPG_API_MAX_POOL_SIZE_ODS: '${NPG_API_MAX_POOL_SIZE_ODS:-20}'
      NPG_API_MAX_POOL_SIZE_ADMIN: '${NPG_API_MAX_POOL_SIZE_ADMIN:-20}'
      NPG_API_MAX_POOL_SIZE_SECURITY: '${NPG_API_MAX_POOL_SIZE_SECURITY:-20}'
      NPG_API_MAX_POOL_SIZE_MASTER: '${NPG_API_MAX_POOL_SIZE_MASTER:-20}'
      ODS_CONNECTION_STRING_ENCRYPTION_KEY: '${ODS_CONNECTION_STRING_ENCRYPTION_KEY}'
      ASPNETCORE_ENVIRONMENT: 'docker'
    volumes:
      - ${LOGS_FOLDER}:/app/logs
      - ./settings/appsettings.v2.dockertemplate.json:/app/appsettings.v2.dockertemplate.json
    entrypoint: ['/bin/sh']
    command:
      [
        '-c',
        'envsubst < /app/appsettings.v2.dockertemplate.json > /app/appsettings.docker.json && /app/run.sh',
      ]
    depends_on:
      - odsV7-adminV2-tenant1-db-ods
      - odsV7-adminV2-tenant2-db-ods
      - odsV7-adminV2-tenant1-db-admin
      - odsV7-adminV2-tenant2-db-admin
    restart: always
    hostname: ${ODS_V7_ADMIN_V2_MULTI_API_VIRTUAL_NAME}
    healthcheck:
      test: ${ODS_V7_ADMIN_V2_MULTI_API_HEALTHCHECK_TEST}
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV2-multi-adminapi:
    image: edfialliance/ods-admin-api:${ADMIN_API_TAG_7X}
    environment:
      ADMIN_WAIT_POSTGRES_HOSTS: 'odsV7-adminV2-tenant1-db-admin odsV7-adminV2-tenant2-db-admin '
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      ASPNETCORE_ENVIRONMENT: multitenantdocker
      AppSettings__DatabaseEngine: PostgreSql
      AppSettings__PathBase: '${ODS_V7_ADMIN_V2_MULTI_ADMIN_API_VIRTUAL_NAME}'
      Authentication__Authority: https://localhost/${ODS_V7_ADMIN_V2_MULTI_ADMIN_API_VIRTUAL_NAME}
      Authentication__IssuerUrl: https://localhost/${ODS_V7_ADMIN_V2_MULTI_ADMIN_API_VIRTUAL_NAME}
      Authentication__SigningKey: ${SIGNING_KEY}
      Authentication__AllowRegistration: true
      AppSettings__DefaultPageSizeOffset: ${PAGING_OFFSET:-0}
      AppSettings__DefaultPageSizeLimit: ${PAGING_LIMIT:-25}
      AppSettings__MultiTenancy: ${MULTITENANCY_ENABLED:-true}
      AppSettings__EnableApplicationResetEndpoint: ${ENABLE_APPLICATION_RESET_ENDPOINT:-true}
      AppSettings__adminApiMode: v2
      AppSettings__EncryptionKey: ${ODS_CONNECTION_STRING_ENCRYPTION_KEY}
      SwaggerSettings__EnableSwagger: true
      SwaggerSettings__DefaultTenant: ${SWAGGER_DEFAULT_TENANT:-tenant1}
      Log4NetCore__Log4NetConfigFileName: ./log4net/log4net.config
      EnableDockerEnvironment: true
      AppSettings__AllowedOrigins: ${ALLOWED_ORIGINS:-https://localhost}
      # Increase rate limiting for Connect/Register endpoint
      IpRateLimiting__GeneralRules__0__Limit: 25
      EdFiApiDiscoveryUrl: ${ODS_V7_ADMIN_V2_MULTI_API_DISCOVERY_URL}
    volumes:
      - ./settings/appsettings.v2.dockertemplate.json:/app/appsettings.v2.dockertemplate.json
      - ./settings/log4net.config:/app/log4net/log4net.config
    entrypoint: ['/bin/sh']
    command:
      [
        '-c',
        'envsubst < /app/appsettings.v2.dockertemplate.json > /app/appsettings.multitenantdocker.json && /app/run.sh',
      ]
    depends_on:
      - odsV7-adminV2-tenant1-db-ods
      - odsV7-adminV2-tenant2-db-ods
      - odsV7-adminV2-tenant1-db-admin
      - odsV7-adminV2-tenant2-db-admin
    restart: always
    hostname: ${ODS_V7_ADMIN_V2_MULTI_ADMIN_API_VIRTUAL_NAME}
    healthcheck:
      test: ${ODS_V7_ADMIN_V2_MULTI_ADMIN_API_HEALTHCHECK_TEST}
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  # Ed-Fi v7.x - Single tenant (Admin API v3)
  odsV7-adminV3-single-db-ods:
    build:
      context: ./DB-Ods
    image: edfiadminapp/db-ods:local
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_PORT: "${POSTGRES_PORT:-5432}"
      TPDM_ENABLED: '${TPDM_ENABLED:-true}'
      MINIMAL_SQL_PATH: "${MINIMAL_SQL_PATH:-/var/opt/pgsql/data/sql-backups/EdFi.Ods.Minimal.Template.sql}"
      POPULATED_SQL_PATH: "${POPULATED_SQL_PATH:-/var/opt/pgsql/data/sql-backups/EdFi.Ods.Populated.Template.sql}"
      EDFI_ODS_DATASET: "${EDFI_ODS_DATASET:-minimal}"
    restart: always
    volumes:
      - vol-odsV7-adminV3-single-db-ods:/var/lib/postgresql/data
      - ${SQL_BACKUPS_FOLDER:-./db-backup}:/var/opt/pgsql/data/sql-backups/:ro
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER} -h localhost -p ${POSTGRES_PORT:-5432}']
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV3-single-db-admin:
    image: edfialliance/ods-admin-api-db:${ADMIN_DB_TAG_7X}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      # The following variables ared used by bootstrap.sh to set up the ODS
      # connection string in the OdsInstances table.
      ODS_PGBOUNCER_PORT: 5432
      ODS_POSTGRES_HOST: odsV7-adminV3-single-db-ods
      NPG_POOLING_ENABLED: '${NPG_POOLING_ENABLED:-true}'
      NPG_API_MAX_POOL_SIZE_ODS: '${NPG_API_MAX_POOL_SIZE_ODS:-20}'
    volumes:
      - ./settings/bootstrap.sh:/docker-entrypoint-initdb.d/2-bootstrap.sh
      - vol-odsV7-adminV3-single-db-admin:/var/lib/postgresql/data
    restart: always
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready  -U ${POSTGRES_USER}']
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV3-single-api:
    image: edfialliance/ods-api-web-api:${ODS_API_TAG_7X}
    environment:
      ADMIN_POSTGRES_HOST: odsV7-adminV3-single-db-admin
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_PORT: 5432
      PATH_BASE: '${ODS_V7_ADMIN_V3_SINGLE_API_VIRTUAL_NAME}'
      TPDM_ENABLED: '${TPDM_ENABLED:-true}'
      ODS_WAIT_POSTGRES_HOSTS: 'odsV7-adminV3-single-db-ods '
      NPG_POOLING_ENABLED: '${NPG_POOLING_ENABLED:-true}'
      NPG_API_MAX_POOL_SIZE_ODS: '${NPG_API_MAX_POOL_SIZE_ODS:-20}'
      NPG_API_MAX_POOL_SIZE_ADMIN: '${NPG_API_MAX_POOL_SIZE_ADMIN:-20}'
      NPG_API_MAX_POOL_SIZE_SECURITY: '${NPG_API_MAX_POOL_SIZE_SECURITY:-20}'
      NPG_API_MAX_POOL_SIZE_MASTER: '${NPG_API_MAX_POOL_SIZE_MASTER:-20}'
      ODS_CONNECTION_STRING_ENCRYPTION_KEY: '${ODS_CONNECTION_STRING_ENCRYPTION_KEY}'
      ASPNETCORE_ENVIRONMENT: 'docker'
      FeatureManagement__MultiTenancy: false
      ApiSettings__OdsContextRouteTemplate: '{instanceId}'
    volumes:
      - ${LOGS_FOLDER}:/app/logs
    depends_on:
      - odsV7-adminV3-single-db-ods
      - odsV7-adminV3-single-db-admin
    restart: always
    hostname: ${ODS_V7_ADMIN_V3_SINGLE_API_VIRTUAL_NAME}
    healthcheck:
      test: ${ODS_V7_ADMIN_V3_SINGLE_API_HEALTHCHECK}
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV3-single-adminapi:
    image: edfialliance/ods-admin-api:${ADMIN_API_TAG_7X}
    environment:
      ADMIN_POSTGRES_HOST: odsV7-adminV3-single-db-admin
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      ASPNETCORE_ENVIRONMENT: docker
      AppSettings__DatabaseEngine: PostgreSql
      AppSettings__PathBase: '${ODS_V7_ADMIN_V3_SINGLE_ADMIN_API_VIRTUAL_NAME}'
      Authentication__Authority: https://localhost/${ODS_V7_ADMIN_V3_SINGLE_ADMIN_API_VIRTUAL_NAME}
      Authentication__IssuerUrl: https://localhost/${ODS_V7_ADMIN_V3_SINGLE_ADMIN_API_VIRTUAL_NAME}
      Authentication__SigningKey: ${SIGNING_KEY}
      Authentication__AllowRegistration: true
      AppSettings__DefaultPageSizeOffset: ${PAGING_OFFSET:-0}
      AppSettings__DefaultPageSizeLimit: ${PAGING_LIMIT:-25}
      AppSettings__MultiTenancy: false
      AppSettings__EncryptionKey: ${ODS_CONNECTION_STRING_ENCRYPTION_KEY}
      SwaggerSettings__EnableSwagger: true
      SwaggerSettings__DefaultTenant: ${SWAGGER_DEFAULT_TENANT:-tenant1}
      Log4NetCore__Log4NetConfigFileName: ./log4net/log4net.config
      EnableDockerEnvironment: true
      AppSettings__AllowedOrigins: ${ALLOWED_ORIGINS:-https://localhost}
      AppSettings__EnableApplicationResetEndpoint: ${ENABLE_APPLICATION_RESET_ENDPOINT:-true}
      AppSettings__adminApiMode: v3
      ConnectionStrings__EdFi_Admin: 'host=odsV7-adminV3-single-db-admin;port=5432;username=${POSTGRES_USER};password=${POSTGRES_PASSWORD};database=EdFi_Admin;'
      ConnectionStrings__EdFi_Security: 'host=odsV7-adminV3-single-db-admin;port=5432;username=${POSTGRES_USER};password=${POSTGRES_PASSWORD};database=EdFi_Security;'
      ConnectionStrings__EdFi_Ods: 'host=odsV7-adminV3-single-db-ods;port=${POSTGRES_PORT:-5432};username=${POSTGRES_USER};password=${POSTGRES_PASSWORD};database=EdFi_Ods;pooling=true'
      ConnectionStrings__EdFi_Master: 'host=odsV7-adminV3-single-db-ods;port=${POSTGRES_PORT:-5432};username=${POSTGRES_USER};password=${POSTGRES_PASSWORD};database=postgres;pooling=true'
      EdFiApiDiscoveryUrl: ${ODS_V7_ADMIN_V3_SINGLE_API_DISCOVERY_URL}
    volumes:
      - ./settings/log4net.config:/app/log4net/log4net.config
    depends_on:
      - odsV7-adminV3-single-db-admin
      - odsV7-adminV3-single-db-ods
    restart: always
    hostname: ${ODS_V7_ADMIN_V3_SINGLE_ADMIN_API_VIRTUAL_NAME}
    healthcheck:
      test: ${ODS_V7_ADMIN_V3_SINGLE_ADMIN_API_HEALTHCHECK}
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  # Ed-Fi v7.x - Multi tenants (Admin API v3)
  odsV7-adminV3-tenant1-db-ods:
    build:
      context: ./DB-Ods
    image: edfiadminapp/db-ods:local
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_PORT: "${POSTGRES_PORT:-5432}"
      TPDM_ENABLED: '${TPDM_ENABLED:-true}'
      MINIMAL_SQL_PATH: "${MINIMAL_SQL_PATH:-/var/opt/pgsql/data/sql-backups/EdFi.Ods.Minimal.Template.sql}"
      POPULATED_SQL_PATH: "${POPULATED_SQL_PATH:-/var/opt/pgsql/data/sql-backups/EdFi.Ods.Populated.Template.sql}"
      EDFI_ODS_DATASET: "${EDFI_ODS_DATASET:-minimal}"
    restart: always
    volumes:
      - vol-odsV7-adminV3-tenant1-db-ods:/var/lib/postgresql/data
      - ${SQL_BACKUPS_FOLDER:-./db-backup}:/var/opt/pgsql/data/sql-backups/:ro
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER} -h localhost -p ${POSTGRES_PORT:-5432}']
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV3-tenant2-db-ods:
    build:
      context: ./DB-Ods
    image: edfiadminapp/db-ods:local
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_PORT: "${POSTGRES_PORT:-5432}"
      TPDM_ENABLED: '${TPDM_ENABLED:-true}'
      MINIMAL_SQL_PATH: "${MINIMAL_SQL_PATH:-/var/opt/pgsql/data/sql-backups/EdFi.Ods.Minimal.Template.sql}"
      POPULATED_SQL_PATH: "${POPULATED_SQL_PATH:-/var/opt/pgsql/data/sql-backups/EdFi.Ods.Populated.Template.sql}"
      EDFI_ODS_DATASET: "${EDFI_ODS_DATASET:-minimal}"
    restart: always
    volumes:
      - vol-odsV7-adminV3-tenant2-db-ods:/var/lib/postgresql/data
      - ${SQL_BACKUPS_FOLDER:-./db-backup}:/var/opt/pgsql/data/sql-backups/:ro
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER} -h localhost -p ${POSTGRES_PORT:-5432}']
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV3-tenant1-db-admin:
    image: edfialliance/ods-admin-api-db:${ADMIN_DB_TAG_7X}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      # The following variables ared used by bootstrap.sh to set up the ODS
      # connection string in the OdsInstances table.
      ODS_PGBOUNCER_PORT: 5432
      ODS_POSTGRES_HOST: odsV7-adminV3-tenant1-db-ods
      NPG_POOLING_ENABLED: '${NPG_POOLING_ENABLED:-true}'
      NPG_API_MAX_POOL_SIZE_ODS: '${NPG_API_MAX_POOL_SIZE_ODS:-20}'
      TENANT_IDENTIFIER: tenant1
    volumes:
      - ./settings/bootstrap.sh:/docker-entrypoint-initdb.d/2-bootstrap.sh
      - vol-odsV7-adminV3-tenant1-db-admin:/var/lib/postgresql/data
    restart: always
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready  -U ${POSTGRES_USER}']
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV3-tenant2-db-admin:
    image: edfialliance/ods-admin-api-db:${ADMIN_DB_TAG_7X}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      ODS_PGBOUNCER_PORT: 5432
      # The following variables ared used by bootstrap.sh to set up the ODS
      # connection string in the OdsInstances table.
      ODS_POSTGRES_HOST: odsV7-adminV3-tenant2-db-ods
      NPG_POOLING_ENABLED: '${NPG_POOLING_ENABLED:-true}'
      NPG_API_MAX_POOL_SIZE_ODS: '${NPG_API_MAX_POOL_SIZE_ODS:-20}'
      TENANT_IDENTIFIER: tenant2
    volumes:
      - ./settings/bootstrap.sh:/docker-entrypoint-initdb.d/2-bootstrap.sh
      - vol-odsV7-adminV3-tenant2-db-admin:/var/lib/postgresql/data
    restart: always
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready  -U ${POSTGRES_USER}']
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV3-multi-api:
    image: edfialliance/ods-api-web-api:${ODS_API_TAG_7X}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_PORT: 5432
      PATH_BASE: '${ODS_V7_ADMIN_V3_MULTI_API_VIRTUAL_NAME}'
      TPDM_ENABLED: '${TPDM_ENABLED:-true}'
      ODS_WAIT_POSTGRES_HOSTS: 'odsV7-adminV3-tenant1-db-ods odsV7-adminV3-tenant2-db-ods '
      NPG_POOLING_ENABLED: '${NPG_POOLING_ENABLED:-true}'
      NPG_API_MAX_POOL_SIZE_ODS: '${NPG_API_MAX_POOL_SIZE_ODS:-20}'
      NPG_API_MAX_POOL_SIZE_ADMIN: '${NPG_API_MAX_POOL_SIZE_ADMIN:-20}'
      NPG_API_MAX_POOL_SIZE_SECURITY: '${NPG_API_MAX_POOL_SIZE_SECURITY:-20}'
      NPG_API_MAX_POOL_SIZE_MASTER: '${NPG_API_MAX_POOL_SIZE_MASTER:-20}'
      ODS_CONNECTION_STRING_ENCRYPTION_KEY: '${ODS_CONNECTION_STRING_ENCRYPTION_KEY}'
      ASPNETCORE_ENVIRONMENT: 'docker'
    volumes:
      - ${LOGS_FOLDER}:/app/logs
      - ./settings/appsettings.v3.dockertemplate.json:/app/appsettings.v3.dockertemplate.json
    entrypoint: ['/bin/sh']
    command:
      [
        '-c',
        'envsubst < /app/appsettings.v3.dockertemplate.json > /app/appsettings.docker.json && /app/run.sh',
      ]
    depends_on:
      - odsV7-adminV3-tenant1-db-ods
      - odsV7-adminV3-tenant2-db-ods
      - odsV7-adminV3-tenant1-db-admin
      - odsV7-adminV3-tenant2-db-admin
    restart: always
    hostname: ${ODS_V7_ADMIN_V3_MULTI_API_VIRTUAL_NAME}
    healthcheck:
      test: ${ODS_V7_ADMIN_V3_MULTI_API_HEALTHCHECK_TEST}
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  odsV7-adminV3-multi-adminapi:
    image: edfialliance/ods-admin-api:${ADMIN_API_TAG_7X}
    environment:
      ADMIN_WAIT_POSTGRES_HOSTS: 'odsV7-adminV3-tenant1-db-admin odsV7-adminV3-tenant2-db-admin '
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      ASPNETCORE_ENVIRONMENT: multitenantdocker
      AppSettings__DatabaseEngine: PostgreSql
      AppSettings__PathBase: '${ODS_V7_ADMIN_V3_MULTI_ADMIN_API_VIRTUAL_NAME}'
      Authentication__Authority: https://localhost/${ODS_V7_ADMIN_V3_MULTI_ADMIN_API_VIRTUAL_NAME}
      Authentication__IssuerUrl: https://localhost/${ODS_V7_ADMIN_V3_MULTI_ADMIN_API_VIRTUAL_NAME}
      Authentication__SigningKey: ${SIGNING_KEY}
      Authentication__AllowRegistration: true
      AppSettings__DefaultPageSizeOffset: ${PAGING_OFFSET:-0}
      AppSettings__DefaultPageSizeLimit: ${PAGING_LIMIT:-25}
      AppSettings__MultiTenancy: ${MULTITENANCY_ENABLED:-true}
      AppSettings__EnableApplicationResetEndpoint: ${ENABLE_APPLICATION_RESET_ENDPOINT:-true}
      AppSettings__adminApiMode: v3
      AppSettings__EncryptionKey: ${ODS_CONNECTION_STRING_ENCRYPTION_KEY}
      SwaggerSettings__EnableSwagger: true
      SwaggerSettings__DefaultTenant: ${SWAGGER_DEFAULT_TENANT:-tenant1}
      Log4NetCore__Log4NetConfigFileName: ./log4net/log4net.config
      EnableDockerEnvironment: true
      AppSettings__AllowedOrigins: ${ALLOWED_ORIGINS:-https://localhost}
      # Increase rate limiting for Connect/Register endpoint
      IpRateLimiting__GeneralRules__0__Limit: 25
      EdFiApiDiscoveryUrl: ${ODS_V7_ADMIN_V3_MULTI_API_DISCOVERY_URL}
    volumes:
      - ./settings/appsettings.v3.dockertemplate.json:/app/appsettings.v3.dockertemplate.json
      - ./settings/log4net.config:/app/log4net/log4net.config
    entrypoint: ['/bin/sh']
    command:
      [
        '-c',
        'envsubst < /app/appsettings.v3.dockertemplate.json > /app/appsettings.multitenantdocker.json && /app/run.sh',
      ]
    depends_on:
      - odsV7-adminV3-tenant1-db-ods
      - odsV7-adminV3-tenant2-db-ods
      - odsV7-adminV3-tenant1-db-admin
      - odsV7-adminV3-tenant2-db-admin
    restart: always
    hostname: ${ODS_V7_ADMIN_V3_MULTI_ADMIN_API_VIRTUAL_NAME}
    healthcheck:
      test: ${ODS_V7_ADMIN_V3_MULTI_ADMIN_API_HEALTHCHECK_TEST}
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  # EdFi v6.x
  v6-db-admin:
    image: edfialliance/ods-admin-api-db:${ADMIN_DB_TAG_6X}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      API_MODE: DistrictSpecific
    restart: always
    volumes:
      - vol-db-admin-6x:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready  -U ${POSTGRES_USER}']
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  # Note: this host name _must_ have `ods_` before the district number. Cannot be `ods-`.
  v6-edfi-ods_255901:
    image: edfialliance/ods-api-db-ods:${ODS_DB_TAG_6X}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      TPDM_ENABLED: '${TPDM_ENABLED:-true}'
      ODS_DB: 'EdFi_Ods_255901'
    restart: always
    volumes:
      - vol-db-ods-6x-255901:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready  -U ${POSTGRES_USER}']
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  # Note: this host name _must_ have `ods_` before the district number. Cannot be `ods-`.
  v6-edfi-ods_255902:
    image: edfialliance/ods-api-db-ods:${ODS_DB_TAG_6X}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      TPDM_ENABLED: '${TPDM_ENABLED:-true}'
      ODS_DB: 'EdFi_Ods_255902'
    restart: always
    volumes:
      - vol-db-ods-6x-255902:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready  -U ${POSTGRES_USER}']
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  v6-api:
    image: edfialliance/ods-api-web-api:${ODS_API_TAG_6X}
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_PORT: 5432
      ODS_POSTGRES_HOST: v6-edfi-{0}
      ADMIN_POSTGRES_HOST: v6-db-admin
      API_MODE: DistrictSpecific
      ApiSettings__PathBase: '${V6_ODS_VIRTUAL_NAME}'
      TPDM_ENABLED: '${TPDM_ENABLED:-true}'
      ODS_WAIT_POSTGRES_HOSTS: 'v6-edfi-ods_255901 v6-edfi-ods_255902 '
      API_HEALTHCHECK_TEST: ${V6_API_HEALTHCHECK_TEST}
      NPG_POOLING_ENABLED: '${NPG_POOLING_ENABLED:-true}'
      NPG_API_MAX_POOL_SIZE_ODS: '${NPG_API_MAX_POOL_SIZE_ODS:-20}'
      NPG_API_MAX_POOL_SIZE_ADMIN: '${NPG_API_MAX_POOL_SIZE_ADMIN:-20}'
      NPG_API_MAX_POOL_SIZE_SECURITY: '${NPG_API_MAX_POOL_SIZE_SECURITY:-20}'
      NPG_API_MAX_POOL_SIZE_MASTER: '${NPG_API_MAX_POOL_SIZE_MASTER:-20}'
    volumes:
      - ${LOGS_FOLDER}:/app/logs
    depends_on:
      - v6-db-admin
      - v6-edfi-ods_255901
      - v6-edfi-ods_255902
    restart: always
    hostname: ${V6_ODS_VIRTUAL_NAME}
    healthcheck:
      test: ${V6_API_HEALTHCHECK_TEST}
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  v6-adminapi:
    image: edfialliance/ods-admin-api:${ADMIN_TAG_6X}
    environment:
      ADMIN_POSTGRES_HOST: v6-db-admin
      POSTGRES_PORT: 5432
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      ASPNETCORE_ENVIRONMENT: docker
      AppSettings__DatabaseEngine: 'PostgreSql'
      AppSettings__ApiStartupType: DistrictSpecific
      AppSettings__ProductionApiUrl: ${API_INTERNAL_URL}
      AppSettings__PathBase: '${V6_ADMIN_API_VIRTUAL_NAME}'
      Authentication__Authority: https://localhost/${V6_ADMIN_API_VIRTUAL_NAME}
      Authentication__IssuerUrl: https://localhost/${V6_ADMIN_API_VIRTUAL_NAME}
      Authentication__SigningKey: ${SIGNING_KEY}
      Authentication__AllowRegistration: true
      SwaggerSettings__EnableSwagger: true
      SwaggerSettings__DefaultTenant: ${SWAGGER_DEFAULT_TENANT:-default}
      AppSettings__DefaultPageSizeOffset: ${PAGING_OFFSET:-0}
      AppSettings__DefaultPageSizeLimit: ${PAGING_LIMIT:-25}
      AppSettings__MultiTenancy: false
      AppSettings__EnableApplicationResetEndpoint: ${ENABLE_APPLICATION_RESET_ENDPOINT:-true}
      AppSettings__adminApiMode: v1
      AppSettings__EncryptionKey: ${ODS_CONNECTION_STRING_ENCRYPTION_KEY}
      AppSettings__EnableAdminConsoleAPI: false
      Log4NetCore__Log4NetConfigFileName: ./log4net/log4net.config
      EnableDockerEnvironment: true
      AppSettings__AllowedOrigins: ${ALLOWED_ORIGINS:-https://localhost}
      EdFiApiDiscoveryUrl: ${ODS_API_62_DISCOVERY}
      Tenants: null
      ConnectionStrings__EdFi_Admin: 'host=v6-db-admin;database=EdFi_Admin;username=${POSTGRES_USER};password=${POSTGRES_PASSWORD}'
      ConnectionStrings__EdFi_Security: 'host=v6-db-admin;database=EdFi_Security;username=${POSTGRES_USER};password=${POSTGRES_PASSWORD}'
    volumes:
      - ./settings/log4net.config:/app/log4net/log4net.config
    depends_on:
      - v6-db-admin
    restart: always
    hostname: ${V6_ADMIN_API_VIRTUAL_NAME}
    healthcheck:
      test: ${V6_ADMIN_API_HEALTHCHECK_TEST}
      start_period: 30s
      retries: 30
      interval: 30s
    networks:
      - edfiadminapp-network

  pgadmin4:
    image: dpage/pgadmin4:9.5.0@sha256:2a830466aafd9cbc2aea0f76ff81a80dbbba819f2f5db7e69cb40e9cbdb6bc7b
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - '127.0.0.1:5050:80'
    volumes:
      - pgadmin-data:/var/lib/pgadmin
      - ./settings/pgadmin-servers.json:/pgadmin4/servers.json
    restart: always
    networks:
      - edfiadminapp-network

networks:
  edfiadminapp-network:
    external: true

volumes:
  vol-odsV7-adminV2-single-db-ods:
    driver: local
    name: vol-odsV7-adminV2-single-db-ods
  vol-odsV7-adminV2-single-db-admin:
    driver: local
    name: vol-odsV7-adminV2-single-db-admin
  vol-odsV7-adminV2-tenant1-db-ods:
    driver: local
    name: vol-odsV7-adminV2-tenant1-db-ods
  vol-odsV7-adminV2-tenant2-db-ods:
    driver: local
    name: vol-odsV7-adminV2-tenant2-db-ods
  vol-odsV7-adminV2-tenant1-db-admin:
    driver: local
    name: vol-odsV7-adminV2-tenant1-db-admin
  vol-odsV7-adminV2-tenant2-db-admin:
    driver: local
    name: vol-odsV7-adminV2-tenant2-db-admin
  vol-odsV7-adminV3-single-db-ods:
    driver: local
    name: vol-odsV7-adminV3-single-db-ods
  vol-odsV7-adminV3-single-db-admin:
    driver: local
    name: vol-odsV7-adminV3-single-db-admin
  vol-odsV7-adminV3-tenant1-db-ods:
    driver: local
    name: vol-odsV7-adminV3-tenant1-db-ods
  vol-odsV7-adminV3-tenant2-db-ods:
    driver: local
    name: vol-odsV7-adminV3-tenant2-db-ods
  vol-odsV7-adminV3-tenant1-db-admin:
    driver: local
    name: vol-odsV7-adminV3-tenant1-db-admin
  vol-odsV7-adminV3-tenant2-db-admin:
    driver: local
    name: vol-odsV7-adminV3-tenant2-db-admin
  vol-db-admin-6x:
    driver: local
    name: vol-db-admin-6x
  vol-db-ods-6x-255901:
    driver: local
    name: vol-db-ods-6x-255901
  vol-db-ods-6x-255902:
    driver: local
    name: vol-db-ods-6x-255902
  pgadmin-data:
    driver: local
    name: pgadmin-data
